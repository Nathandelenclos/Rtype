name: Repo Mirror CI

on:
  push:

jobs:
  to_epitech:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - uses: pixta-dev/repository-mirroring-action@v1
        with:
          target_repo_url: "git@github.com:EpitechPromo2026/B-CPP-500-LIL-5-2-rtype-david.plouvier.git"
          ssh_private_key:
            ${{ secrets.PRIVATE_SSH_KEY }}
